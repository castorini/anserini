conditions:
  - name: bm25
    display: "BM25 (k1=0.9, b=0.4)"
    display_html: "BM25 (<i>k<sub><small>1</small></sub></i>=0.9, <i>b</i>=0.4)"
    display_row: ""
    command: java -cp $fatjar --add-modules jdk.incubator.vector io.anserini.search.SearchCollection -threads $threads -index cacm -topics $topics -output $output -hits 1000 -bm25
    topics:
      - topic_key: cacm
        eval_key: cacm
        expected_scores:
          MAP: 0.3123
          P30: 0.1942
        metric_definitions:
          MAP: "-c -m map"
          P30: "-c -m P.30"
